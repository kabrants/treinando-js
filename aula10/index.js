/** 
 *Aritméticos + - / *
 * + Adição / Concatenação  (- / *)
 * ** Potenciação
 * % Resto da divisão
 * () primeiro os parentes são executados primeiros
 * () ** * / % + - ordem 
 * Incremento = ++
 * Decremento = --
 * Operadores de atribuição
 * NaN = Not a number
 * parseInt retorna um número inteiro / parseFloat retorna um número com pontuação flutuante
 * Number retorna inteiro ou decimal(flutuante)
 */

const num1 = 10;
const num2 = Number('5.2');
console.log (num1 + num2);
